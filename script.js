let headsTails = Math.round(Math.random());
console.log(headsTails);

// 1 or 0 has been generated and so now need to get it into an array
//which will then send the img tag a src of either the first or
//second array item which is the heads or tails photo
